const Sign = require('../../models/Sign');

const Signup = async (req, res) => {

    console.log(req);
    // const {email, password} = req.body;

    // try {
    //     const log = await sign.findOne({email});
    //     console.log(log);
    // }
    // catch(error) {
    //     console.log(error);
    // }
}

module.exports = {
    Signup
}